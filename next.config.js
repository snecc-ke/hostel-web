/** @type {import('next').NextConfig} */
// Use Next's default dev bundler (Webpack) to avoid runtime issues with native
// modules like Prisma while developing. Turbopack may not resolve certain
// server-side native bindings in dev.
const nextConfig = {};

module.exports = nextConfig;
